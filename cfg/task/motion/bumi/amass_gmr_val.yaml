# @package task.command

windowed_next_window_device: current
windowed_pin_window_load: true
motion_cfgs:
  bumi_amass_gmr_val:
    path: ".cache/mimic-lite/motions/bumi/amass_gmr_val"
    weight: 1.0
    full_motion: true
    shard: false
